# WattWise 수정 작업 정리

## 1. 전력 대시보드

- 가전별 사용량 예측 섹션을 대시보드 하단으로 이동했습니다.
- `household_chronos2`의 Chronos-2 가정 전체 사용량 예측을 대시보드 상단의 가정 선택과 연결했습니다.
- 가정 선택 목록은 전체 가구를 가정 번호의 숫자 순으로 표시합니다.
- 화면에는 학습용·예측용 구분 문구를 노출하지 않으며, 선택한 가구의 최근 관측값으로 미래를 예측합니다.
- 가정을 선택하면 최근 168시간 관측으로 향후 24시간을 예측합니다.
- 24시간 중앙 예측값을 30일로 환산해 다음 KPI를 갱신합니다.
  - 이번 달 예상 사용량
  - 예상 청구요금
  - 현재 누진 구간
  - 다음 누진 구간까지 남은 사용량
- EDA는 기존 가상 차트 대신 개발 과정 벤치마크 실측 데이터에서 가구와 기간을 선택해 분석하도록 변경했습니다.
  - 시간대별 평균
  - 요일별 평균
  - 일별 추세
  - 요일 × 시간 히트맵
- 상단의 불필요한 데이터 성격 안내 문구와 `합성 NILM` 표현을 제거했습니다.

## 2. 태양광·거래

- 상단 메뉴의 `태양광·거래`를 `태양광`과 `거래` 하위 메뉴로 분리했습니다.
- 태양광 페이지는 가정용으로 정리하고 기업·공공기관 선택을 제거했습니다.
- 손익분기 계산기를 태양광 페이지로 옮겼습니다.
- 거래 페이지에 모의 거래 플랫폼을 추가했습니다.
  - 구매·판매 전환
  - 지정가·시장가 주문
  - 구매대기·판매대기
  - 부분 체결 및 자동 체결
  - 주문 취소와 예약 금액·전력량 반환
  - 금액 기준 수량 계산
  - 호가창, 가격 차트, 잔고, 주문 내역
  - 계정별 브라우저 저장
- 실제 전력거래소나 결제 시스템은 연결하지 않았으며, 가상 시세와 가상 잔고를 사용하는 모의 거래입니다.

## 3. 스마트홈 진단·이상탐지

- 별도 이상탐지 메뉴를 스마트홈 진단 내부로 통합했습니다.
- 가정 A~E를 선택하면 각 가정별 사용량·요금·이상탐지 패널을 확인할 수 있습니다.
- `exp.py`의 합성 이상 유형을 시나리오로 추가했습니다.
  - 정상
  - TV·조명 상시 ON
  - 취사 부하 소실
  - 냉장고만·활동정지
  - 야간 활동 소실
  - 외출·여행 예외
- 기존 `anomaly` 페이지 주소는 스마트홈 진단으로 이동하도록 처리했습니다.
- 기존 방법론·PoC 설명은 각 가정의 이상탐지 패널 안에서 펼쳐볼 수 있습니다.

## 4. 이상탐지 실제 모델 연결

- `exp.py`를 오토인코더 체크포인트 저장·로드 구조로 변경했습니다.
- 최초 실행 시 합성 정상 데이터로 학습하고 아래 정보를 저장합니다.
  - PyTorch `state_dict`
  - 정규화 값 `mu`, `sd`
  - 이상 임계값
  - 입력 길이와 모델 버전
- 이후 실행은 저장된 체크포인트를 로드하므로 재학습하지 않습니다.
- 생성된 체크포인트:
  - `anomaly_output/anomaly_checkpoint.pt`
- 스마트홈 진단은 `/api/anomaly`를 통해 체크포인트를 로컬에서 로드하고 복원 오차와 임계값을 계산합니다.
- 현재 모델은 `exp.py`의 합성 1인가구 데이터 기반 PoC입니다. 실제 가정별 실측 데이터로 재학습하는 과정은 아직 별도 작업입니다.

## 5. 소개 페이지

- `수정 중.pptx`의 정책·연구 배경·차별점·로드맵을 소개 페이지에 반영했습니다.
- Policy 섹션에 다음 내용을 추가했습니다.
  - 유럽·영국의 실시간 전력 거래
  - 한국 AMI 약 2,005만 호
  - 아파트형 VPP
  - 단지 내 전력 공유
- 사업자 중심 거래, 중앙 관리형 데이터, 기능 파편화라는 기존 한계를 정리했습니다.
- Trade·Save·Protect 세 가지 핵심 가치를 시각적으로 재구성했습니다.
- 온디바이스 방식과 4단계 개발 로드맵을 추가했습니다.
- 데스크톱과 모바일 레이아웃을 확인했습니다.

## 6. 주요 파일

- `src/app/(portal)/analytics/page.tsx`: 전력 대시보드 및 EDA
- `src/app/api/household-forecast/route.ts`: 가정별 Chronos-2 예측 API
- `src/app/api/benchmark/route.ts`: 개발 과정 벤치마크 API
- `src/app/(portal)/trade/`: 태양광 모의 거래 페이지
- `src/lib/trading.ts`: 주문·체결·예약·취소 도메인 로직
- `src/app/(portal)/households/page.tsx`: 스마트홈 진단
- `src/lib/householdAnomaly.ts`: 가정별 이상 시나리오 생성
- `src/app/api/anomaly/route.ts`: 오토인코더 이상 점수 API
- `household_chronos2/anomaly_inference.py`: 체크포인트 로드 및 이상 점수 계산
- `anomaly_output/anomaly_checkpoint.pt`: 저장된 오토인코더 체크포인트
- `exp.py`: 합성 데이터 생성·오토인코더 학습·평가·체크포인트 저장

## 7. 실행 방법

```bash
npm install
npm run dev
```

로컬 주소: `http://localhost:3000`

로그인 계정 예시:

```text
아이디: korea_03
비밀번호: korea
```

이상탐지 체크포인트를 강제로 다시 학습하려면:

```bash
.venv-dashboard/bin/python exp.py --force-train
```

## 8. 설치한 Python 패키지

`household_chronos2/requirements.txt`에 다음 항목을 추가했습니다.

```text
matplotlib>=3.7
scikit-learn>=1.3
```

기존 Chronos-2, PyTorch, pandas, NumPy, PyArrow, holidays 의존성도 유지됩니다.

## 9. 검증 결과

- TypeScript 검사 통과
- 변경 파일 ESLint 검사 통과
- 거래 도메인 테스트 8개 통과
- 가정별 이상탐지 테스트 4개 통과
- 가정 A~E 이상탐지 시나리오 전환 브라우저 테스트 통과
- 거래 페이지 구매·판매·시장가·지정가·대기·취소·새로고침 유지 테스트 통과
- EDA API 89가구 데이터 로드 및 가구·기간 변경 확인
- household Chronos-2 예측 API에서 24개 예측 결과 반환 확인
- anomaly API에서 체크포인트 로드 후 실제 복원 오차 반환 확인

## 10. 데이터 관련 참고

- 개발 과정 데이터 벤치마크에는 AI Hub 실측 자료로 표기된 89가구 시간별 값이 포함되어 있습니다.
- `household_chronos2/data/hourly_base.parquet`에는 가정 전체 Chronos-2 예측에 사용하는 16가구가 있습니다.
- 전력 대시보드의 요금 계산과 가전별 사용량 분해는 별도의 도메인 시뮬레이션입니다.
- 이상탐지 체크포인트는 합성 데이터 기반 PoC이며, 실측 가정 데이터 기반 성능으로 해석하면 안 됩니다.

## 11. 모델 가중치와 Git LFS

- 가전별 Chronos-2 가중치 4개와 가정 전체 예측용 가중치 1개는 각각 약 456MB입니다.
- GitHub 일반 파일 제한(100MB)을 넘기므로 `.safetensors` 파일은 Git LFS로 관리합니다.
- 기존 가전별 4개 파일은 이미 LFS 추적 상태이며, 새로 추가한 파일도 다음 경로로 LFS에 등록했습니다.
  - `household_chronos2/checkpoint/model.safetensors`
- `anomaly_output/anomaly_checkpoint.pt`는 약 52KB라 일반 Git 파일로 관리합니다.
- 모델 5개의 총 용량은 약 2.28GB이므로 GitHub 계정의 LFS 저장공간·대역폭 한도를 확인해야 합니다.

## 12. GitHub Pages 배포 상태

- 소스 코드는 `master` 브랜치에 있고, 라이브 데모는 `gh-pages` 브랜치에서 제공됩니다.
- 현재 자동 배포 workflow는 없으므로 `master`에 push해도 라이브 데모가 자동으로 갱신되지 않습니다.
- `gh-pages`의 마지막 배포본이 이전 커밋을 가리키고 있어 현재 라이브 주소에는 최신 수정사항이 반영되지 않은 상태입니다.
- 현재 앱은 API 라우트를 포함하고 `next.config.mjs`에 정적 export 설정이 없으므로, GitHub Pages에 올리려면 정적 export 대응 작업이 필요합니다.
- 전체 기능(API 포함)을 유지하려면 Vercel처럼 Next.js 서버를 실행할 수 있는 배포 환경을 사용해야 합니다.
